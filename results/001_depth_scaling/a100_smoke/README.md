# A100 smoke results

Hardware: NVIDIA A100 80GB PCIe. The original Pod was the A100 run; its host location is not used for cross-hardware latency comparisons.

These are two fixed GSM8K examples across `D={4,8,16,32,64}`. They are smoke-test artifacts, not a benchmark result.

The original top-level files are preserved for backward compatibility.
