# H100 smoke results

Hardware: NVIDIA H100 80GB HBM3, US-MO-1, driver 580.126.09. Listed secure Pod rate: $3.49/hour.

These results use the exact same two GSM8K examples, prompt, model revision, dtype, depth sweep, and generation settings as `a100_smoke`. Latencies are **not directly comparable** to A100 because the GPU hardware differs.

This is a smoke-test artifact, not a benchmark result.
